<div>
  <h1 class="font-bold">New Post</h1>
  <div>
    <%= form_with model: @post do |form| %>
      <%= form.label :body %>
      <%= form.text_area :body %>
      <%= form.submit "Create New Post" %>
    <% end %>
  </div>
</div>
